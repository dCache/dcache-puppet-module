# Managed by Puppet (dcache::array)
# Do NOT edit, changes will be overwritten!

devices {
    device {
        vendor                "DELL"
        product               "MD34xx"
        path_grouping_policy  group_by_prio
        prio                  rdac
        path_checker          rdac
        path_selector         "round-robin 0"
        hardware_handler      "1 rdac"
        failback              immediate
        features              "2 pg_init_retries 50"
        no_path_retry         30
        rr_min_io             100
    }
}

defaults {
    find_multipaths yes
    user_friendly_names yes
}

blacklist {
}
