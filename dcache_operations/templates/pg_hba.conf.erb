######################################################################
##
## DO NOT EDIT
## generated by puppet module :  <%= @name %>
## node                       :  <%= @fqdn %>
##
######################################################################
# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# This file controls: which hosts are allowed to connect, how clients
# are authenticated, which PostgreSQL user names they can use, which
# databases they can access.  Records take one of these forms:
#
# host       DATABASE  USER  ADDRESS  METHOD  [OPTIONS]
# hostssl    DATABASE  USER  ADDRESS  METHOD  [OPTIONS]
# hostnossl  DATABASE  USER  ADDRESS  METHOD  [OPTIONS]
#
# (The uppercase items must be replaced by actual values.)
#
local	all	all	trust
<% @pg_hba_conf.each do |host_name,connection_properties| -%>
<%= connection_properties['TYPE'] %>	<%= connection_properties['DATABASE'] %>	<%= connection_properties['USER'] %>	<%= connection_properties['ADDRESS'] %>	<%= connection_properties['METHOD'] %>	#<%= host_name %>
<% end -%>
